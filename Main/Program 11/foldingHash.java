public class foldingHash { //folding method for hasing by Asta Walor-Scott
	public static class key {
		private String key;
		int uniqueCount;
		

		key (String key,int secondKey){
			this.key = key;
			this.uniqueCount = secondKey;

		}
	}
	
	public void setFold(String key, int secondKey) {
		int size = 9491;
	}
	
	//need to get count
	private int getCount(String key) {
		char[] temp = key.toCharArray();
		int number = temp.length;
		
		while (number > ) {
			
		}
		
		return number;
	}
}
